# How to create shell-commands

create a small programm using the emexOS libc and compile it into x86_64_elf then move your elf file to the /bin folder, then start emexOS and type in the name of the file without ".elf"
